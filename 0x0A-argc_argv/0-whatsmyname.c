#include <stdio.h>
#include "main.h"

/**
 * main - this is a function to print its name
 * @argc: count argument
 * @argv: arguments
 * Return: 0 for success
 */

int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", *argv);

return (0);
}
